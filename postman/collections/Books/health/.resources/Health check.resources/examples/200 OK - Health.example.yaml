$kind: http-example
request:
  url: "{{baseUrl}}/health"
  method: GET
  body:
    type: json
    content: ""
response:
  statusCode: 200
  statusText: OK
  body:
    type: json
    content: |-
      {
        "status": "{{mockHealthStatus}}"
      }
order: 3000
